[project]
name = "traderai-worker"
version = "0.1.0"
requires-python = ">=3.13,<3.14"
dependencies = ["celery==5.6.3", "redis>=7.1.0", "traderai-domain"]
[dependency-groups]
dev = ["pytest>=9.0.2", "ruff>=0.14.8", "mypy>=1.19.0"]
[tool.uv.sources]
traderai-domain = { workspace = true }
